"use strict";

exports.__esModule = true;
exports.useIndexBar = useIndexBar;

var _components = require("../utils/components");

var _provide = require("../index-bar/provide");

function useIndexBar() {
  var {
    parentProvider,
    bindParent
  } = (0, _components.useParent)(_provide.INDEX_BAR_BIND_INDEX_ANCHOR_KEY);
  var {
    index
  } = (0, _components.useAtParentIndex)(_provide.INDEX_BAR_COUNT_INDEX_ANCHOR_KEY);

  if (!parentProvider || !bindParent) {
    throw Error('[Varlet] IndexAnchor: You should use this component in "IndexBar"');
  }

  return {
    index,
    indexBar: parentProvider,
    bindIndexBar: bindParent
  };
}