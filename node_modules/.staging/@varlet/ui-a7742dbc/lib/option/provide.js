"use strict";

exports.__esModule = true;
exports.useSelect = useSelect;

var _components = require("../utils/components");

var _provide = require("../select/provide");

function useSelect() {
  var {
    bindParent,
    parentProvider
  } = (0, _components.useParent)(_provide.SELECT_BIND_OPTION_KEY);
  var {
    index
  } = (0, _components.useAtParentIndex)(_provide.SELECT_COUNT_OPTION_KEY);

  if (!bindParent || !parentProvider) {
    throw Error('<var-option/> must in <var-select/>');
  }

  return {
    index,
    select: parentProvider,
    bindSelect: bindParent
  };
}