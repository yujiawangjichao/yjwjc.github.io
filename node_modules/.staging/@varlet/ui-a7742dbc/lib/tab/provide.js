"use strict";

exports.__esModule = true;
exports.useTabs = useTabs;

var _components = require("../utils/components");

var _provide = require("../tabs/provide");

function useTabs() {
  var {
    parentProvider,
    bindParent
  } = (0, _components.useParent)(_provide.TABS_BIND_TAB_KEY);
  var {
    index
  } = (0, _components.useAtParentIndex)(_provide.TABS_COUNT_TAB_KEY);

  if (!parentProvider || !bindParent || !index) {
    throw Error('<var-tab/> must in <var-tabs/>');
  }

  return {
    index,
    tabs: parentProvider,
    bindTabs: bindParent
  };
}