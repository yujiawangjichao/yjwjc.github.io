"use strict";

exports.__esModule = true;
exports.useCollapse = useCollapse;

var _components = require("../utils/components");

var _provide = require("../collapse/provide");

function useCollapse() {
  var {
    parentProvider,
    bindParent
  } = (0, _components.useParent)(_provide.COLLAPSE_BIND_COLLAPSE_ITEM_KEY);
  var {
    index
  } = (0, _components.useAtParentIndex)(_provide.COLLAPSE_COUNT_COLLAPSE_ITEM_KEY);

  if (!parentProvider || !bindParent || !index) {
    throw Error('[Varlet] Collapse: <var-collapse-item/> must in <var-collapse>');
  }

  return {
    index,
    collapse: parentProvider,
    bindCollapse: bindParent
  };
}