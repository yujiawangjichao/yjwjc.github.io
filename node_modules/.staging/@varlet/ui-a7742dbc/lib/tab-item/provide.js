"use strict";

exports.__esModule = true;
exports.useTabsItems = useTabsItems;

var _components = require("../utils/components");

var _provide = require("../tabs-items/provide");

function useTabsItems() {
  var {
    parentProvider,
    bindParent
  } = (0, _components.useParent)(_provide.TABS_ITEMS_BIND_TAB_ITEM_KEY);
  var {
    index
  } = (0, _components.useAtParentIndex)(_provide.TABS_ITEMS_COUNT_TAB_ITEM_KEY);

  if (!parentProvider || !bindParent || !index) {
    throw Error('<var-tab-item/> must in <var-tabs-items/>');
  }

  return {
    index,
    tabsItems: parentProvider,
    bindTabsItems: bindParent
  };
}