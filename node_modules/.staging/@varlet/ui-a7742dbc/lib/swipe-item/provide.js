"use strict";

exports.__esModule = true;
exports.useSwipe = useSwipe;

var _components = require("../utils/components");

var _provide = require("../swipe/provide");

function useSwipe() {
  var {
    bindParent,
    parentProvider
  } = (0, _components.useParent)(_provide.SWIPE_BIND_SWIPE_ITEM_KEY);
  var {
    index
  } = (0, _components.useAtParentIndex)(_provide.SWIPE_COUNT_SWIPE_ITEM_KEY);

  if (!bindParent || !parentProvider || !index) {
    throw Error('<var-swipe-item/> must in <var-swipe/>');
  }

  return {
    index,
    swipe: parentProvider,
    bindSwipe: bindParent
  };
}