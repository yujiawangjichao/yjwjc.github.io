"use strict";

exports.__esModule = true;
exports.useRow = useRow;

var _components = require("../utils/components");

var _provide = require("../row/provide");

function useRow() {
  var {
    parentProvider,
    bindParent
  } = (0, _components.useParent)(_provide.ROW_BIND_COL_KEY);
  var {
    index
  } = (0, _components.useAtParentIndex)(_provide.ROW_COUNT_COL_KEY);

  if (!parentProvider || !bindParent || !index) {
    console.warn('col must in row');
  }

  return {
    index,
    row: parentProvider,
    bindRow: bindParent
  };
}